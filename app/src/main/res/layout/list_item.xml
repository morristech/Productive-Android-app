<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:minHeight="@dimen/project_list_tile_height"
    android:layout_height="@dimen/project_list_tile_height"
    android:background="@android:color/white"
    android:paddingLeft="@dimen/smaller_spacing">

    <LinearLayout
        android:id="@+id/content_layout"
        android:layout_width="match_parent"
        android:minHeight="@dimen/project_list_tile_height"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="@dimen/default_spacing"
        android:layout_toRightOf="@+id/item_thumbnail"
        android:orientation="vertical"
        android:background="@drawable/item_card_border"
        android:paddingBottom="@dimen/default_spacing"
        android:paddingTop="@dimen/default_spacing"
        android:paddingRight="@dimen/default_spacing">

        <TextView
            android:id="@+id/item_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:lines="1"
            android:textColor="@color/projectTitleColor"
            android:textSize="@dimen/title_text_size"
            tools:text="This is something we're trying to test"/>

        <TextView
            android:id="@+id/item_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/nano_spacing"
            android:textColor="@color/projectAndSectionDescriptionColor"
            android:textSize="@dimen/small_text_size"
            tools:text="Updated 3 hours ago by Mislav J."/>

    </LinearLayout>

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@id/item_thumbnail"
        android:layout_width="@dimen/project_list_avatar_pic_width_height"
        android:layout_height="@dimen/project_list_avatar_pic_width_height"
        android:layout_centerVertical="true"
        android:layout_marginRight="@dimen/project_list_avatar_margin_right"
        android:src="@mipmap/ic_launcher"
        app:civ_border_color="@android:color/white"
        app:civ_border_width="@dimen/project_list_circleview_border_width"/>

</RelativeLayout>


